services:
  erp:
    image: ghcr.io/qadirmansoor/erp-zimaos:latest
    build: .
    container_name: erp
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - erp-data:/data
    environment:
      - PORT=3000

volumes:
  erp-data:
